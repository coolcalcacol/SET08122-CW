/** @format */

/**
 * All the difficulty levels that I currently provide.
 */
enum Difficulty {
	Easy = 'Easy',
	Medium = 'Medium',
	Hard = 'Hard',
	Extreme = 'Extreme',
	Impossible = 'Impossible',
}

enum MaxValues {
	Easy = 0.45,
	Medium = 0.55,
	Hard = 0.65,
	Extreme = 0.75,
	Impossible = 0.85,
}

enum MinValues {
	Easy = 0.4,
	Medium = 0.5,
	Hard = 0.6,
	Extreme = 0.7,
	Impossible = 0.8,
}

export { Difficulty, MaxValues, MinValues };
